# location flow

## 1. when in incident mode
        user gets a sliding screen w following options:
            1) fetch live
            2) use permanent
            3) use recents (3 locations max)
            